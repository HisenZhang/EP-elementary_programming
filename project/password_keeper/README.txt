我的password keeper主要是实现了一个简易的备忘录的功能，可以通过设置用户名和相应密码来通过if语句的判断，输入密码的过程通过getpass来实现输入不可见，如果密码输入正确，登录后查看备忘录中的内容，当密码输入错误后会进入一段已经写好相应内容的对话，主要是一电脑输出语句和读取用户输入的回答，最后是通过sys.exit自动退出程序，在备忘录中可以分段输出储存的内容，最后是可以通过用户输出的exit指令经过if的判定后来进行sys.exit，或是输出一条可以自定义的装饰性语句来完成一个加密的密码备忘录的功能和一段较为人性化的提示对话